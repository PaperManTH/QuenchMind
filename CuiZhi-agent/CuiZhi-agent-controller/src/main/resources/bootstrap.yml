server:
  port: 18025

spring:
  application:
    name: agent-service

  ai:
    dashscope:
        api-key: sk-xxxxxxxxxxxxxx

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/quenchmind?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: postgres
    password: postgres

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

#  cloud:
#    nacos:
#      config:
#        enabled: true # 保持开启，但因为上面写了 optional，所以不会报错
#        server-addr: localhost:8848
#      discovery:
#        enabled: false

logging:
  level:
    com.cuizhi: debug
