spring:
  application:
    name: cuizhi-gateway

  profiles:
    active: dev

  config:
    import:
      - optional:nacos:cuizhi-gateway-dev.yaml

  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: 6155490e-eda2-446d-b5de-85d176fb72cf
        group: QuenchMind
        file-extension: yaml
        refresh-enabled: true

      discovery:
        server-addr: localhost:8848
        namespace: 6155490e-eda2-446d-b5de-85d176fb72cf
        group: QuenchMind